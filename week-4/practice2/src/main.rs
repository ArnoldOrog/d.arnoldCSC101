fn main() {
    let empty_string= String::new(); //this code allows for an empty string to be made for pushing values e.t.c 
    println!("length of empty_string is {}",empty_string.len());
    let content_string = String::from("Computer Science");
    println!("Length of content_string is {}",content_string.len());//.len means it should print the length of the string
}
